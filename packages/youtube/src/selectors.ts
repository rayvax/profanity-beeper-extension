export const CaptionSelector = {
  ROOT: '.ytp-caption-window-container',
  SEGMENT: '.ytp-caption-segment',
} as const;

export const PlayerSelector = {
  CONTAINER: '.html5-video-player',
  VIDEO: 'video.video-stream',
} as const;
