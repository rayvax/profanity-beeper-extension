# Bundle the initial speech model

The initial Russian Vosk model ships as extension data instead of being downloaded when ML mode is enabled. This keeps the censorship pipeline local and available offline, trading a larger extension package for deterministic installation and no model-hosting dependency.
