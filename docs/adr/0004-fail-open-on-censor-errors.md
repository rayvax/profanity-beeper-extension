# Fail open on censor errors

If a transcript source, speech model, or playback graph fails, the extension restores normal playback instead of blocking video. The resulting Censor status is visible in the extension action and popup so the user knows censorship is inactive.
